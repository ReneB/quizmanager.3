<%= simple_form_for(@content_image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :question %>
    <%= f.association :answer %>
    <%= f.input :file_path %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
